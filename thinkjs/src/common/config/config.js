/**
 * config
 */
export default {
  host: "127.0.0.1",
  // 内部端口81**开头
  port: 8100,
  // 开启自定义路由
  route_on: false,
  default_module: 'core', //默认模块
  default_controller: 'connection',  //默认的控制器
  default_action: 'get', //默认的 Action
};
